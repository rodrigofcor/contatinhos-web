import { Rancho, Ubuntu } from 'next/font/google'

export const ubuntu = Ubuntu({
  weight: '400',
  subsets: ['latin'],
  variable: '--font-ubuntu',
})

export const rancho = Rancho({
  weight: '400',
  subsets: ['latin'],
  variable: '--font-rancho',
})